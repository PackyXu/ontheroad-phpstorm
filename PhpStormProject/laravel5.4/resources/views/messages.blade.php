@extends('layouts.app')@section('content')    <h1>Contact meaages</h1>    @if(count ($messages) > 0)    <div class="col-xs-12 col-sm-12 col-md-8 col-md-offset-2">        <ul class="list-group">            @foreach($messages as $message)            <li class="list-group-item">                <p>{{ $message->id }}</p>                <p>{{ $message->name }}</p>                <p>{{ $message->email }}</p>                <p>{{ $message->title }}</p>                <p>{{ $message->content }}</p>            </li>            @endforeach        </ul>    </div>    @endif@endsection